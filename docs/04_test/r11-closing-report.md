# R11 记忆增益对照实验 — 收官报告（建议书）

> **提交对象**：豆包管家项目负责人 / 评审相关方
> **性质**：实验收官 + 下一步裁决建议
> **日期**：2026-09-12
> **实验归属**：AutoFlow 竞技场（Arena）A/B 对照 — 记忆增益假设验证

---

## 一、结论

🟡 **数据不足（环境阻塞），退判重域（deferred）**。

- 未下任何产品结论。
- **不推论**记忆机制「有效」或「无效」——本轮回合因环境缺陷，记忆变量被完全遮蔽，无法分离记忆效应。

---

## 二、五阶段全流程（t1 → t5 全完成，reviewer verdict = pass）

| 阶段 | 成员 | 结果 | 要点 |
|---|---|---|---|
| t1 环境核对 | recon | ✅ | 确认两臂各 179 台 `synced_from_ha` 设备；发现旧题库 5/6 题实体失效；端点契约偏差 |
| t2 A 臂裸跑 | arm-a | ✅ | 0/6 fully_verified（V1+V2 双题库落档） |
| t3 B 臂记忆跑 | arm-b | ✅ | 0/6 fully_verified；灵感 30 条，0 采纳 |
| t4 指标汇总 | metrics | ✅ | 四指标表 + FINDINGS §R11（4 条） |
| t5 独立复核 | reviewer | ✅ | 5 项验收全过，数值可复算，证据链完整 |

> 流程闭环完整， reviewer 判定 `pass`；结论受限的是**数据**，不是流程。

---

## 三、核心发现（FINDINGS.md §R11）

- **F-R11-01（P1 · 阻塞）**：vhass staging 设备目录同步缺陷——两臂 179 台 `synced_from_ha=true` 但 staging 目录缺 `switch`/`light` 实体，`submit` 全部 gate fail。唯一可比题 **T2（温度超 29℃ 开风扇）** 两臂 DSL 一致、拦截原因完全对称 → **memory 变量被环境完全遮蔽**。
- **F-R11-02（P2）**：A/B 两臂题集不一致（A 用 V2，B 脚本硬编码 V1）。
- **F-R11-03（P3）**：灵感 snapshot 命中率 0%（含 1 条 event 红线被正确拒绝）。
- **F-R11-04（P3）**：KICKOFF §4 端点命名（`propose_task`/`submit_flow`）与实际（`propose`/`submit`）不符。

---

## 四、四指标对照

| 指标 | 结果 | 判定 |
|---|---|---|
| M1 一次通过率 | 0% = 0% | ✗ 受阻塞 |
| M2 验证步数 | B/A = 2.0 | ✗ 受阻塞 |
| M3 复犯失败 | 0 = 0 | ✗ 受阻塞 |
| M4 灵感采纳率 | 0%（报告项） | — 有效可比题 N=1 < 3，命中数据不足 |

三项核心指标**全部受阻塞干扰**；有效可比题 **N=1 < 3**，样本量不满足下结论的门槛。

---

## 五、给项目负责人的行动项

1. **修复 vhass staging 目录同步（F-R11-01，阻塞性）**：核查「首用懒创建」为何未覆盖 `synced_from_ha` 设备——这是本回合数据失效的根因。
2. **统一两臂题集为 V2 后重跑对照**：消除 F-R11-02 的题集不一致变量，使 A/B 仅差「记忆」一项。
3. （待定）**采纳 reviewer 整改建议**后再重启，见第六节。

---

## 六、Reviewer 附：3 项非阻断整改建议

1. **evidence 路径修正** — FINDINGS 中若干证据路径引用需对齐实际落盘位置。
2. **A 臂 submit 次数表述** — 实际 submit ≥ 4 次，原文表述偏低，需修正。
3. **N<3 判据注明出处** — 「有效可比题不足 3 题即退判」的阈值依据应在报告中显式标注来源。

---

## 七、关键交付文件

- `results/r11-summary.md`
- `results/r11-taskset-v2.json`
- `FINDINGS.md` §R11
- `results/r11-captain-probe-*.json`
- `results/r11-a-*` / `results/r11-b-*`（全量原始 JSON）

---

## 八、待裁决事项

> **需要我按 reviewer 建议修正 FINDINGS 路径引用，还是等 staging 修复后重启对照？**

两条路径不互斥，但顺序建议：
- 先修 **F-R11-01**（staging 同步）才是让实验「可比」的前提；
- 路径引用修正（F-R11-04 / reviewer 建议 1）可并行，不依赖环境。

---

## 交付文件（本次归档）

**已交付**：
- `results/r11-summary.md`
- `results/r11-taskset-v2.json`
- `FINDINGS.md`
- `results/r11-a-execution-report.md`

**交付完成声明**：R11 对照实验全流程已收尾归档，结论与证据链齐全，等待负责人裁决下一步（修 staging / 统一题集重跑 / 采纳 reviewer 整改建议）。
